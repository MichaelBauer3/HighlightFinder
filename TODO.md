# TODO - Not prioritized
1. Add automatic messaging that sends files and alerts of status to messenger gc
2. Add scheduling using CRON to run these jobs automatically with as close to 0 monitoring
3. Consider adding a way to not start screen recording until the scoreboard reads 0 - 0 and 25:00 or 18:00?
4. Since the model cannot hadel double digits, set up a rollover setting that automatically knows its if at 9 and read 0,add to first digit
5. Consider taking requests from a channel to clips the requested timestamp (Dribbles, Saves, Ref Calls, etc...)
6. Make all the selenium methods more reliable (they fail occasionally)
7. Make sure that the clip files are small enough to be sent while maintaining their quality

## Categories
1. Scoreboard Enhancements (3, 4, 5)
2. QOL (1, 2, 6, 7)

### Prioritized
1. Add scheduling to run automatically
2. Make sure that the clip files are small enough to be sent while maintaining their quality
3. Add Messaging to the gc